# Examples — Summit Dumpster Rental (anonymized)

These are real deliverables with the client, people, locations, account IDs, and
competitor names changed. Numbers and reasoning are preserved.

## The worked example (week of 2026-06-26)

A complete, real same-week handoff from analysis to client delivery:

1. **`SummitDumpster_MAA_2026-06-26.md`** — the MAA from `google-ads-analyzer`.
2. **`SummitDumpster_Client-View_2026-06-26.md` / `.html`** — the
   `google-ads-client-view` render of that MAA. The `.html` is the Basecamp-paste
   version. This is genuine production output, not a reconstruction.
3. **`SummitDumpsterRental_chart_*_2026-06-26.png`** — the two trend charts,
   produced by `render_charts.py` from **`trend.csv`** (also included so you can
   see the chart input format). The chart endpoints match the MAA's headline
   numbers (8 leads, $75.89 this week, $79.55 30-day).

## Additional skill samples

Output formats for the two skills that didn't fire that week (a clean, under-target
week needed no new copy or negatives). These are separate samples from an earlier
cycle of the same account:

- **`SummitDumpster_AdCopy_2026-04-24.md`** — `google-ads-copy-optimizer` output.
- **`SummitDumpster_Negatives-Round4_v2.js`** — `google-ads-change-scripts` output
  (dry-run first; account ID and competitor names are fictional placeholders).
